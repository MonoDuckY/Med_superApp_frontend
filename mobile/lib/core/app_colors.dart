import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary Colors
  static const Color medicalBlue = Color(0xFF1560BD);
  static const Color teal = Color(0xFF06B6D4);
  static const Color deepNavy = Color(0xFF0B2167);

  // Background and Canvas Colors
  static const Color canvasColor = Color(0xFFF0F4F8); // Light blue-gray canvas
  static const Color white = Colors.white;
  static const Color black = Colors.black;

  // Text Colors
  static const Color textPrimary = Color(0xFF1A202C);
  static const Color textSecondary = Color(0xFF718096);

  // Status Colors
  static const Color error = Color(0xFFE53E3E);
  static const Color success = Color(0xFF38A169);
  static const Color warning = Color(0xFFD69E2E);
}
